all:
  hosts:
    switch1:
      ansible_host: 10.10.20.35
      ansible_user: "{{ lookup('env','ANSIBLE_USER') }}"
      ansible_password: "{{ lookup('env','ANSIBLE_PASS') }}"
      ansible_network_os: ios
      ansible_connection: network_cli